import React from 'react';
import InfoIdentityView from './InfoIdentityView';

interface Props {}

const InfoIdentityScreen: React.FC = (props: Props) => {
  return <InfoIdentityView />;
};

export default InfoIdentityScreen;
