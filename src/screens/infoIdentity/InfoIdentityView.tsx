import React from 'react';
import { StyleSheet, Text } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const InfoIdentityView: React.FC = (props: Props) => {
  return (
    <Container>
      <Text>InfoIdentity Screen </Text>
    </Container>
  );
};

export default InfoIdentityView;

const styles = StyleSheet.create({});
