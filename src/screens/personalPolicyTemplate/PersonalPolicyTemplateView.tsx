import React from "react";
import { StyleSheet, Text } from "react-native";
import { Container } from "../../components/container";

interface Props {};

const PersonalPolicyTemplateView: React.FC = (props: Props) => {
    return (
        <Container>
            <Text>PersonalPolicyTemplate</Text>
        </Container>
    )
};

export default PersonalPolicyTemplateView;

const styles = StyleSheet.create({});