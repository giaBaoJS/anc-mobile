import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const UserSettingLayoutView = (props: Props) => {
  return (
    <Container>
      <Text>UserSettingLayout Screen</Text>
    </Container>
  );
};

export default UserSettingLayoutView;

const styles = StyleSheet.create({});
