import React from 'react';
import SetLoginView from './SetLoginView';

interface Props {}

const SetLoginScreen: React.FC = (props: Props) => {
  return <SetLoginView />;
};

export default SetLoginScreen;
