import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const TransactionRoleView = (props: Props) => {
  return (
    <Container>
      <Text>TransactionRole Screen</Text>
    </Container>
  );
};

export default TransactionRoleView;

const styles = StyleSheet.create({});
