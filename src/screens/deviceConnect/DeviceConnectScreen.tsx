import React from 'react';
import DeviceConnectView from './DeviceConnectView';

interface Props {}

const DeviceConnectScreen: React.FC = (props: Props) => {
  return <DeviceConnectView />;
};

export default DeviceConnectScreen;
