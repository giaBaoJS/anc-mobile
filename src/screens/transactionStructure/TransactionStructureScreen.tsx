import React from 'react';
import { View } from 'react-native';
import TransactionStructureView from './TransactionStructureView';

const TransactionStructureScreen: React.FC = () => {
  return <TransactionStructureView />;
};

export default TransactionStructureScreen;
