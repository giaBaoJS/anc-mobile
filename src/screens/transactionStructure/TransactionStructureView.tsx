import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const TransactionStructureView = (props: Props) => {
  return (
    <Container>
      <Text>TransactionStructure Screen</Text>
    </Container>
  );
};

export default TransactionStructureView;

const styles = StyleSheet.create({});
