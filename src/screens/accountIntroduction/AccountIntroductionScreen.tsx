import React from 'react';
import AccountIntroductionView from './AccountIntroductionView';

interface Props {}

const AccountIntroductionScreen: React.FC = (props: Props) => {
  return <AccountIntroductionView />;
};

export default AccountIntroductionScreen;
