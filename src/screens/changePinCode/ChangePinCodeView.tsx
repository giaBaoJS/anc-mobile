import React from "react";
import { StyleSheet, Text } from "react-native";
import { Container } from "../../components/container";

interface Props {};

const ChangePinCodeView: React.FC = (props: Props) => {
    return (
        <Container>
            <Text>ChangePinCode Screen</Text>
        </Container>
    )
};

export default ChangePinCodeView;

const styles = StyleSheet.create({});