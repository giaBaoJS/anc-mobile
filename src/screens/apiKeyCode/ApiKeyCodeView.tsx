import React from "react";
import { StyleSheet, Text } from "react-native";
import { Container } from "../../components/container";

interface Props {};

const ApiKeyCodeView: React.FC = (props: Props) => {
    return (
        <Container>
            <Text>ApiKeyCode Screen</Text>
        </Container>
    )
};

export default ApiKeyCodeView;

const styles = StyleSheet.create({});