import React from 'react';
import CompositeFrameView from './CompositeFrameView';


interface Props {}

const CompositeFrameScreen: React.FC = (props: Props) => {
  return (
    <CompositeFrameView />
  )
};

export default CompositeFrameScreen;
