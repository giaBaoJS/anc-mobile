import React from 'react';
import { View } from 'react-native';
import TransactionSourceView from './TransactionSourceView';

const TransactionSourceScreen: React.FC = () => {
  return <TransactionSourceView />;
};

export default TransactionSourceScreen;
