import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const TransactionSourceView = (props: Props) => {
  return (
    <Container>
      <Text>TransactionSource Screen</Text>
    </Container>
  );
};

export default TransactionSourceView;

const styles = StyleSheet.create({});
