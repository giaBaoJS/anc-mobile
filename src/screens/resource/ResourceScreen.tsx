import React from 'react';
import { View } from 'react-native';
import ResourceScreenView from './ResourceScreenView';

const ResourceScreenScreen: React.FC = () => {
  return <ResourceScreenView />;
};

export default ResourceScreenScreen;
