import React from 'react';
import BlockProjectsView from './BlockProjectsView';

interface Props {}

const BlockProjectsScreen: React.FC = (props: Props) => {
  return <BlockProjectsView />;
};

export default BlockProjectsScreen;
