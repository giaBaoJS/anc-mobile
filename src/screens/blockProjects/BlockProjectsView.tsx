import React from 'react';
import { StyleSheet, Text } from 'react-native';
import { Container } from '../../components/container';

interface Props {}

const BlockProjectsView: React.FC = (props: Props) => {
  return (
    <Container>
      <Text>BlockProjects Screen</Text>
    </Container>
  );
};

export default BlockProjectsView;

const styles = StyleSheet.create({});
