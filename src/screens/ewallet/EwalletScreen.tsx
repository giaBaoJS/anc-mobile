import React from 'react';
import EwalletView from './EwalletView';

interface Props {}

const EwalletScreen: React.FC = (props: Props) => {
  return <EwalletView />;
};

export default EwalletScreen;
