import React from 'react';
import AddressBookView from './AddressBookView';


interface Props {}

const AddressBookScreen: React.FC = (props: Props) => {
  return (
    <AddressBookView />
  )
};

export default AddressBookScreen;
