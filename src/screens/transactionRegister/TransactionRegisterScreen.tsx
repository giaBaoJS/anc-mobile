import React from 'react';
import { View } from 'react-native';
import TransactionRegisterView from './TransactionRegisterView';

const TransactionRegisterScreen: React.FC = () => {
  return <TransactionRegisterView />;
};

export default TransactionRegisterScreen;
