import React from 'react';
import { View } from 'react-native';
import SynthesisView from './SynthesisView';

const SynthesisScreen: React.FC = () => {
  return <SynthesisView />;
};

export default SynthesisScreen;
